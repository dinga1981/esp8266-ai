# 版本记录

本记录依据本机保存的源码快照和发布说明重建。`v0.5.1` 至 `v0.5.7` 的 Git 提交日期
是历史导入日期，不代表最初开发日期。

## v0.5.13-test（开发中）

- 桥接程序短暂失联时不再主动调用 Wi-Fi 重连或无线重置，只记录失败并继续轮询；无线恢复仅由真实的 Wi-Fi 断线状态触发。
- 任何 Wi-Fi 断线或固件重连动作都会立即暂停HTTP请求，重新获得IP后再等待5秒，避免无线协议栈切换期间与K线请求重叠。
- K线元数据轮询间隔从1秒调整为2秒，降低ESP8266网络栈持续压力，同时保留及时切换能力。
- 本测试版只需更新ESP8266固件，Mac桥接程序继续使用v0.5.12。

## v0.5.12

- 修复正式打包的 macOS 桥接程序无法从标准 `Contents/Resources` 目录定位 SwiftPM 图片资源、双击后立即退出的问题。
- K线行情新增 ESP8266 专用的16色固定颜色表与4位索引RLE协议，保留红涨绿跌、网格、文字和强调色，同时显著降低完整画面的连续内存需求。
- Mac桥接程序继续保留旧RGB565 K线接口，新固件优先使用调色板接口，便于旧工具和预览兼容。
- K线页涨跌幅从10点半粗体放大为16点粗体，并改为右对齐，提高240×240实屏可读性。
- Wi-Fi诊断分别保存首次和最后断线原因，并标记断线来自外部/AP/协议栈还是固件恢复动作，避免主动重连产生的原因8覆盖根因。
- 断线证据新增断线前RSSI、信道、BSSID、持续时间，以及快速重连、无线重置、射频重启和保护性重启时间线。
- RTC诊断继续避开eboot的OTA指令区；设备网页和诊断API同步展示新增证据。
- 本版本需要同时升级Mac桥接程序和ESP8266固件。

## v0.5.11-test（开发中）

- 修复 RTC 诊断记录覆盖 ESP8266 eboot OTA 引导指令、导致上传成功但重启后仍停留旧版本的问题；等待 OTA 重启期间暂停其他任务。
- RTC 诊断新增保存重启前的 Wi-Fi 断线原因、恢复步骤、断线/重连次数、桥接失败次数和 HTTP 结果，重启后仍可追溯。
- 自动轮播模式下改为记录屏幕当时真正显示的页面，不再只记录“自动”。
- Wi-Fi 持续断线 3 分钟时，在保护性重启之前增加一次无线射频关闭/唤醒恢复；5 分钟仍未恢复才重启。
- 设备诊断页和 `/api/diagnostics` 新增上述重启前证据。
- 本测试版只需更新 ESP8266 固件，Mac 桥接程序保持 v0.5.9。

## v0.5.10-test（开发中）

- 设备网页改为从 Flash 分段发送，HTTP JSON 改为流式解析，并减少高频动态字符串，降低 ESP8266 堆内存峰值和碎片。
- 诊断内存改为空闲周期采样，增加最低空闲内存、最大连续内存块和碎片率。
- ESP8266 显式启用 Wi-Fi 自动重连并关闭无线省电休眠，改善部分路由器上的长期关联与 ARP 稳定性。
- 新增分级自恢复：Wi-Fi 断线后依次快速重连、重新初始化无线网络，持续 5 分钟仍失败才保护性重启。
- 桥接程序连续失败时只各执行一次快速重连和无线重置，不因 Mac 休眠或退出而循环重启设备。
- 设备网页新增“立即重连 Wi-Fi”，不清除 Wi-Fi、轮播和桌宠设置。
- 诊断页新增复位原因、累计启动次数、重启前运行位置、Wi-Fi 断线原因、断线/重连次数、桥接失败次数、HTTP 结果和最近恢复动作。
- 通过 RTC 内存保存重启前正在执行的网络阶段，辅助判断看门狗或异常复位发生在桥接、天气、行情、K 线、网页或 OTA 流程。
- 本测试版只需更新 ESP8266 固件，Mac 桥接程序保持 v0.5.9。

## v0.5.9

- Mac 菜单新增完整设置导出和一键恢复，覆盖四行报价、K 线收藏与周期、天气城市，以及设备亮度、显示模式和工作日/周末轮播方案。
- 备份明确排除 Wi-Fi 密码、Codex/Claude 登录凭据、缓存和桌宠二进制文件；恢复时自动使用当前 Mac 的桥接地址。
- 设备网页新增 ESP8266 网络 OTA 升级，包含文件类型、可用空间、固件头和写入结果校验；正常升级保留 Wi-Fi、LittleFS 和已有设置。
- 设备网页新增诊断区，显示 Wi-Fi 信号、桥接连接、数据更新时间、固件/桥接版本、Heap、LittleFS、运行时间及 OTA 空间，不加入屏幕轮播。
- Codex 页重置月日改用更清晰的高对比实心字体。
- 天气页放大城市和日期，并将城市、星期、天气状况及今日/明日标签全部改为中文；中文字由 Mac 预渲染后传给 ESP8266，避免占用大体积中文字库。
- Mac 桥接程序与 ESP8266 固件需要同时升级。

## v0.5.8

- Codex 额度页顶部新增准确的下次重置日期和时间，同时保留右侧剩余天数/小时。
- 桥接程序保留 Codex 返回的绝对重置时间戳，并向 ESP8266 传递重置时刻对应的本地时区偏移。
- Mac 左键预览同步新的重置时间布局。
- 工作日和周末自动轮播时间新增 15 秒、20 秒选项。
- Mac 桥接程序与 ESP8266 固件需要同时升级。

## v0.5.7

- 新增日期天气页，显示城市、日期时间、当前天气，以及今日和明日预报。
- 新增工作日/周末两套自动轮播方案，分别配置页面和 5/10/30/60/120 秒间隔。
- K 线页仍会等待全部收藏完整展示一轮后再离开。
- 暂时从菜单和轮播设置中隐藏 Claude 与网速页面，保留底层代码。
- Mac 桥接程序与 ESP8266 固件需要同时升级。

## v0.5.6

- Mac 左键预览同步 ESP8266 的 Nothing 风格额度页。
- 同步双列/单列布局、RESET 时间和额度耗尽倒计时。
- 仅需更新 Mac 桥接程序。

## v0.5.5

- 移植上游 `v0.4.10`、`v0.4.11` 的 Claude/Codex 额度页改进。
- 新增 Nothing 风格点阵字体和周额度重置时间。
- Mac 端保存最近一次成功额度，改善冷启动、网络失败和限流期间的显示。
- Mac 桥接程序与 ESP8266 固件需要同时升级。

## v0.5.4

- 修复伦敦金始终被跳过、美元指数偶尔被跳过的问题。
- 为伦敦金和美元指数增加备用 K 线数据源。
- 修复自定义期货收藏在桥接程序重启后的恢复逻辑。
- 仅需更新 Mac 桥接程序。

## v0.5.3

- 伦敦现货黄金改用真实 OHLC 分钟数据，显示 1/5/60 分钟蜡烛 K 线。
- 已收藏的伦敦金无需重新添加。
- 仅需更新 Mac 桥接程序。

## v0.5.2

- 修复外层自动轮播在 K 线收藏尚未完整展示时提前离开 K 线页的问题。
- K 线页停留时间改为等待全部收藏标的完整轮换一遍。
- Mac 桥接程序与 ESP8266 固件需要同时升级。

## v0.5.1

- 新增 K 线收藏删除功能，至少保留一个标的。
- 自动模式改为可配置页面轮播。
- 新增 5/10/30/60/120 秒页面切换间隔。
- 轮播设置保存到 ESP8266 LittleFS。

## v0.5.0

只保留了早期 ESP8266 固件和 Web 刷机包，未找到可核验的完整源码快照，因此不重建
对应源码提交和标签。相关二进制文件可作为历史存档提供，但不应视为可复现源码版本。
